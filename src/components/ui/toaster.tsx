import * as React from "react";

/**
 * Minimal Toaster (stub)
 * - Formål: Få build/deploy til at virke uden ekstra UI-dependencies.
 * - Du kan senere udskifte den med en “rigtig” shadcn/radix toaster, hvis du vil.
 */
export function Toaster() {
  return null;
}

export default Toaster;
